program IdeMenuTest;

uses Ide.Menu, Std.Test;

begin
  AssertEquals(1, CmdFileExit);
  AssertEquals(10, CmdHelpAbout);
  AssertEquals('Exit', CommandLabel(CmdFileExit));
  AssertEquals('About', CommandLabel(CmdHelpAbout));
  AssertEquals('Unknown', CommandLabel(999))
end.
