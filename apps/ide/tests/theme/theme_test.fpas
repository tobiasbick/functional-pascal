program IdeThemeTest;

uses Ide.Theme, Std.Console, Std.Test;

begin
  AssertEquals(Blue, DesktopBg);
  AssertEquals(Black, ChromeFg);
  AssertEquals(LightGray, ChromeBg);
  AssertEquals(Red, AccentFg)
end.
