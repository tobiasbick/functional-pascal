program T;

type
  Config = record
    Host: string;
    Port: integer;
    Retries: integer;
    TimeoutSeconds: integer;
  end;

begin
  var C: Config := record
    Host := 'api.example.com';
    Port := 443;
    Retries := 5;
    TimeoutSeconds := 30;
  end
end.
