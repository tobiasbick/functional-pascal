# Deferred debugger backlog

No independently useful debugger capability package is currently deferred.

Implemented behavior and explicit limitations belong in
[`docs/pascal/tools/`](../../pascal/tools/debugger.md), not in this backlog.

## Maintenance rules

- Keep one row per independently specifiable unresolved package.
- Add a stable ID, boundary, re-entry gate, acceptance criteria, and resumable
  evidence before implementation starts.
- Completing or rejecting a package removes its row.
- Do not preserve implementation history or rejected approaches as backlog.
