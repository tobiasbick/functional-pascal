{ Spec: docs/pascal/std/env.md (from repository root). }
program EnvBasics;
uses Std.Console, Std.Env, Std.Option;

begin
  WriteLn('PATH set: ', Exists('PATH'));
  case Get('PATH') of
    Some(_): WriteLn('PATH lookup: some');
    None:    WriteLn('PATH lookup: none')
  end
end.
