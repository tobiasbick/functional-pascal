{ Spec: docs/pascal/std/proc.md — requires `fpas` on PATH. }
program ProcBasics;
uses Std.Console, Std.Conv, Std.Proc, Std.Result;

begin
  case Run('fpas', ['--version']) of
    Ok(Code): WriteLn('fpas --version exit: ', IntToStr(Code));
    Error(M): WriteLn('run error: ', M)
  end
end.
