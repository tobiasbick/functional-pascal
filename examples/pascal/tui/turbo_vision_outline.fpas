{ Turbo Vision outline tree demo. Run in a terminal; press Close to exit. }

program TurboVisionOutlineDemo;

uses Std.Tui;

function Bounds(X: integer; Y: integer; Width: integer; Height: integer): Rect;
begin
  return record x := X; y := Y; width := Width; height := Height; end
end;

function Leaf(Text: string): OutlineNode;
begin
  return record text := Text; children := []; expanded := false; end
end;

function Node(Text: string; Children: array of OutlineNode; Expanded: boolean): OutlineNode;
begin
  return record text := Text; children := Children; expanded := Expanded; end
end;

procedure OnCommand(App: Application; CommandId: integer);
begin
  if CommandId = CM_QUIT then
  begin
    Application.Quit(App)
  end
end;

begin
  var App: Application := Application.Open();
  var Roots: array of OutlineNode := [
                                       Node('demo', [Node('src', [Leaf('main.fpas'), Leaf('lib.fpas')], true), Leaf('README.md')], true)

                                     ];
  var Win: Window := Window.New(Bounds(8, 3, 44, 14), 'Project tree');
  var TreeHandle: Outline := Outline.New(Bounds(3, 2, 36, 10), Roots);
  var CloseButton: Button := Button.New(Bounds(14, 12, 12, 2), 'Close', CM_QUIT, false);
  Window.Add(Win, TreeHandle);
  Window.Add(Win, CloseButton);
  Desktop.Add(App, Win);
  Application.Run(App, OnCommand);
  Application.Close(App)
end.
