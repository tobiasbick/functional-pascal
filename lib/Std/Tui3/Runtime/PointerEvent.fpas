unit Std.Tui3.Runtime.PointerEvent;

uses Std.Console, Std.Tui3.Geometry.Point;

type
  /// Pointer button carried by a normalized pointer event.
  TuiPointerButton = enum
    NoButton;
    Left;
    Right;
    Middle;
  end;
  /// Normalized pointer action in zero-based application coordinates.
  TuiPointerEvent = record
    Action: Std.Console.MouseAction;
    Button: TuiPointerButton;
    Position: TuiPoint;
    Shift: boolean;
    Ctrl: boolean;
    Alt: boolean;
    Meta: boolean;

    /// Creates a pointer event with no modifiers.

    static function Create(Action: Std.Console.MouseAction; Button: TuiPointerButton; Position: TuiPoint): TuiPointerEvent;
    begin
      return record
        Action := Action;
        Button := Button;
        Position := Position;
        Shift := false;
        Ctrl := false;
        Alt := false;
        Meta := false;
      end
    end;

    /// Creates a pointer event with explicit modifiers.

    static function WithModifiers(
  Action: Std.Console.MouseAction;
  Button: TuiPointerButton;
  Position: TuiPoint;
  Shift: boolean;
  Ctrl: boolean;
  Alt: boolean;
  Meta: boolean
): TuiPointerEvent;
    begin
      return record
        Action := Action;
        Button := Button;
        Position := Position;
        Shift := Shift;
        Ctrl := Ctrl;
        Alt := Alt;
        Meta := Meta;
      end
    end;
  end;
