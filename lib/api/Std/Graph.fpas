unit Std.Graph;

public type
  // Generated by `cargo run -p fpas-sema --example export_intrinsic_std_api`.
  // Edit the Rust registry and `docs/pascal/std/`, then regenerate this editor declaration surface.
  // Runtime behavior is implemented by the compiler, VM, or Rust standard-library crate.

  // Opaque graphics-session handle.
  Application = record
    // Fill the runtime backbuffer.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `Color`: Color value used by the operation.
    public static procedure Clear(App: Std.Graph.Application; Color: integer);
    begin
      panic('Intrinsic API declaration')
    end;

    // Close the session.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    public static procedure Close(App: Std.Graph.Application);
    begin
      panic('Intrinsic API declaration')
    end;

    // Register hosted handlers.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `Handlers`: `Std.Graph.ApplicationHandlers` input value used by `Configure`.
    public static procedure Configure(App: Std.Graph.Application; Handlers: Std.Graph.ApplicationHandlers);
    begin
      panic('Intrinsic API declaration')
    end;

    // Draw one clipped circle outline.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `CenterX`: Horizontal coordinate.
    // - `CenterY`: Vertical coordinate.
    // - `Radius`: `integer` input value used by `DrawCircle`.
    // - `Color`: Color value used by the operation.
    public static procedure DrawCircle(
  App: Std.Graph.Application;
  CenterX: integer;
  CenterY: integer;
  Radius: integer;
  Color: integer
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Draw one clipped line.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `X1`: Horizontal coordinate.
    // - `Y1`: Vertical coordinate.
    // - `X2`: Horizontal coordinate.
    // - `Y2`: Vertical coordinate.
    // - `Color`: Color value used by the operation.
    public static procedure DrawLine(
  App: Std.Graph.Application;
  X1: integer;
  Y1: integer;
  X2: integer;
  Y2: integer;
  Color: integer
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Draw one clipped rectangle outline.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `X`: Horizontal coordinate.
    // - `Y`: Vertical coordinate.
    // - `Width`: Width in cells or pixels as documented by the operation.
    // - `Height`: Height in cells or pixels as documented by the operation.
    // - `Color`: Color value used by the operation.
    public static procedure DrawRect(
  App: Std.Graph.Application;
  X: integer;
  Y: integer;
  Width: integer;
  Height: integer;
  Color: integer
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Draw deterministic bitmap text.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `X`: Horizontal coordinate.
    // - `Y`: Vertical coordinate.
    // - `Text`: UTF-8 text processed by the operation.
    // - `Color`: Color value used by the operation.
    public static procedure DrawText(App: Std.Graph.Application; X: integer; Y: integer; Text: string; Color: integer);
    begin
      panic('Intrinsic API declaration')
    end;

    // Fill one clipped rectangle.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `X`: Horizontal coordinate.
    // - `Y`: Vertical coordinate.
    // - `Width`: Width in cells or pixels as documented by the operation.
    // - `Height`: Height in cells or pixels as documented by the operation.
    // - `Color`: Color value used by the operation.
    public static procedure FillRect(
  App: Std.Graph.Application;
  X: integer;
  Y: integer;
  Width: integer;
  Height: integer;
  Color: integer
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostDispatchRedraw` function.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    public static function HostDispatchRedraw(App: Std.Graph.Application): integer;
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostProcessNext` function.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `MaxSpins`: `integer` input value used by `HostProcessNext`.
    public static function HostProcessNext(App: Std.Graph.Application; MaxSpins: integer): integer;
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnCloseRequested` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `OnCloseRequested`: `procedure(App: Std.Graph.Application)` input value used by `HostRegisterOnCloseRequested`.
    public static procedure HostRegisterOnCloseRequested(
  App: Std.Graph.Application;
  OnCloseRequested: procedure(App: Std.Graph.Application)
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnExit` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `OnExit`: `procedure(App: Std.Graph.Application; Reason: Std.Graph.ExitReason)` input value used by `HostRegisterOnExit`.
    public static procedure HostRegisterOnExit(
  App: Std.Graph.Application;
  OnExit: procedure(App: Std.Graph.Application; Reason: Std.Graph.ExitReason)
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnIdle` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `Milliseconds`: Non-negative duration in milliseconds.
    // - `OnIdle`: `procedure(App: Std.Graph.Application)` input value used by `HostRegisterOnIdle`.
    public static procedure HostRegisterOnIdle(
  App: Std.Graph.Application;
  Milliseconds: integer;
  OnIdle: procedure(App: Std.Graph.Application)
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnKeyPressed` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `OnKeyPressed`: `function(App: Std.Graph.Application; Key: Std.Console.KeyEvent): boolean` input value used by `HostRegisterOnKeyPressed`.
    public static procedure HostRegisterOnKeyPressed(
  App: Std.Graph.Application;
  OnKeyPressed: function(App: Std.Graph.Application; Key: Std.Console.KeyEvent): boolean
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnMouse` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `OnMouse`: `procedure(App: Std.Graph.Application; Event: Std.Graph.Event)` input value used by `HostRegisterOnMouse`.
    public static procedure HostRegisterOnMouse(
  App: Std.Graph.Application;
  OnMouse: procedure(App: Std.Graph.Application; Event: Std.Graph.Event)
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnPaint` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `OnPaint`: `procedure(App: Std.Graph.Application)` input value used by `HostRegisterOnPaint`.
    public static procedure HostRegisterOnPaint(
  App: Std.Graph.Application;
  OnPaint: procedure(App: Std.Graph.Application)
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnResize` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `OnResize`: `procedure(App: Std.Graph.Application; NewSize: Std.Graph.Size)` input value used by `HostRegisterOnResize`.
    public static procedure HostRegisterOnResize(
  App: Std.Graph.Application;
  OnResize: procedure(App: Std.Graph.Application; NewSize: Std.Graph.Size)
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `HostRegisterOnWheel` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `OnWheel`: `procedure(App: Std.Graph.Application; Event: Std.Graph.Event)` input value used by `HostRegisterOnWheel`.
    public static procedure HostRegisterOnWheel(
  App: Std.Graph.Application;
  OnWheel: procedure(App: Std.Graph.Application; Event: Std.Graph.Event)
);
    begin
      panic('Intrinsic API declaration')
    end;

    // Cooperative quit.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    public static procedure HostRequestQuit(App: Std.Graph.Application);
    begin
      panic('Intrinsic API declaration')
    end;

    // Open one native window.
    //
    // Parameters:
    // - `Width`: Width in cells or pixels as documented by the operation.
    // - `Height`: Height in cells or pixels as documented by the operation.
    // - `Title`: Window title displayed by the host.
    public static function Open(Width: integer; Height: integer; Title: string): Std.Graph.Application;
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `OpenForTest` function.
    //
    // Parameters:
    // - `Width`: Width in cells or pixels as documented by the operation.
    // - `Height`: Height in cells or pixels as documented by the operation.
    public static function OpenForTest(Width: integer; Height: integer): Std.Graph.Application;
    begin
      panic('Intrinsic API declaration')
    end;

    // Flush the runtime backbuffer.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    public static procedure Present(App: Std.Graph.Application);
    begin
      panic('Intrinsic API declaration')
    end;

    // Write one clipped pixel.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `X`: Horizontal coordinate.
    // - `Y`: Vertical coordinate.
    // - `Color`: Color value used by the operation.
    public static procedure PutPixel(App: Std.Graph.Application; X: integer; Y: integer; Color: integer);
    begin
      panic('Intrinsic API declaration')
    end;

    // Request `OnPaint`.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    public static procedure RequestRedraw(App: Std.Graph.Application);
    begin
      panic('Intrinsic API declaration')
    end;

    // Hosted main loop.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    public static procedure Run(App: Std.Graph.Application);
    begin
      panic('Intrinsic API declaration')
    end;

    // Current drawable size.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    public static function Size(App: Std.Graph.Application): Std.Graph.Size;
    begin
      panic('Intrinsic API declaration')
    end;

    // Runs the intrinsic `TestSendKey` procedure.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `Key`: Lookup key or keyboard event, according to the operation.
    public static procedure TestSendKey(App: Std.Graph.Application; Key: Std.Console.KeyEvent);
    begin
      panic('Intrinsic API declaration')
    end;

    // Bulk row-major `$00RRGGBB` upload.
    //
    // Parameters:
    // - `App`: Graphics application session receiving the operation.
    // - `Width`: Width in cells or pixels as documented by the operation.
    // - `Height`: Height in cells or pixels as documented by the operation.
    // - `Pixels`: Row-major pixel buffer uploaded to the host.
    public static procedure UploadFrame(
  App: Std.Graph.Application;
  Width: integer;
  Height: integer;
  Pixels: array of integer
);
    begin
      panic('Intrinsic API declaration')
    end;
  end;
  // Bundled hosted handler registration.
  ApplicationHandlers = record
    public OnPaint: procedure(App: Std.Graph.Application);
    public OnKeyPressed: option of function(App: Std.Graph.Application; Key: Std.Console.KeyEvent): boolean;
    public OnMouse: option of procedure(App: Std.Graph.Application; Event: Std.Graph.Event);
    public OnWheel: option of procedure(App: Std.Graph.Application; Event: Std.Graph.Event);
    public OnResize: option of procedure(App: Std.Graph.Application; NewSize: Std.Graph.Size);
    public OnCloseRequested: option of procedure(App: Std.Graph.Application);
    public OnIdleMilliseconds: integer;
    public OnIdle: option of procedure(App: Std.Graph.Application);
    public OnExit: option of procedure(App: Std.Graph.Application; Reason: Std.Graph.ExitReason);
  end;
  // Event record with size, key, mouse, wheel, and modifier fields.
  Event = record
    public kind: Std.Graph.EventKind;
    public size: Std.Graph.Size;
    public key: Std.Console.KeyEvent;
    public mouse_action: Std.Console.MouseAction;
    public mouse_button: Std.Console.MouseButton;
    public mouse_x: integer;
    public mouse_y: integer;
    public wheel_x: integer;
    public wheel_y: integer;
    public shift: boolean;
    public ctrl: boolean;
    public alt: boolean;
    public meta: boolean;
  end;
  // `CloseRequested`, `Resize`, `Key`, `Mouse`, `Wheel`.
  EventKind = enum
    // `CloseRequested` enum member.
    CloseRequested;
    // `Resize` enum member.
    Resize;
    // `Key` enum member.
    Key;
    // `Mouse` enum member.
    Mouse;
    // `Wheel` enum member.
    Wheel;
  end;
  // Why `Application.Run` stopped.
  ExitReason = enum
    // `UserQuit` enum member.
    UserQuit;
    // `WindowClosed` enum member.
    WindowClosed;
    // `HostStop` enum member.
    HostStop;
    // `HostAndUserStop` enum member.
    HostAndUserStop;
    // `HostShutdown` enum member.
    HostShutdown;
  end;
  // Record with `width` and `height`.
  Size = record
    public width: integer;
    public height: integer;
  end;
