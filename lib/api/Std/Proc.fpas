unit Std.Proc;

public type
  // Generated by `cargo run -p fpas-sema --example export_intrinsic_std_api`.
  // Edit the Rust registry and `docs/pascal/std/`, then regenerate this editor declaration surface.
  // Runtime behavior is implemented by the compiler, VM, or Rust standard-library crate.

  // Intrinsic `ProcessOutput` data type.
  ProcessOutput = record
    public ExitCode: integer;
    public Stdout: string;
    public Stderr: string;
  end;

// Returns the absolute path of the running FPAS host executable.
public function CurrentExecutable(): result of string, string;
begin
  panic('Intrinsic API declaration')
end;

// Starts a process, waits for completion, and returns the exit code.
//
// Parameters:
// - `Command`: Executable name or path to start.
// - `Args`: Command-line arguments in invocation order.
public function Run(Command: string; Args: array of string): result of integer, string;
begin
  panic('Intrinsic API declaration')
end;

// Starts a process and captures its exit code and output.
//
// Parameters:
// - `Command`: Executable name or path to start.
// - `Args`: Command-line arguments in invocation order.
public function RunCapture(Command: string; Args: array of string): result of Std.Proc.ProcessOutput, string;
begin
  panic('Intrinsic API declaration')
end;
