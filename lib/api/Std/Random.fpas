unit Std.Random;

// Generated by `cargo run -p fpas-sema --example export_intrinsic_std_api`.
// Edit the Rust registry and `docs/pascal/std/`, then regenerate this editor declaration surface.
// Runtime behavior is implemented by the compiler, VM, or Rust standard-library crate.

// Pseudo-random value in `[0.0, 1.0)`.
public function Random(): real;
begin
  panic('Intrinsic API declaration')
end;

// Unbiased pseudo-random value in inclusive `[Lo, Hi]`.
//
// Parameters:
// - `Lo`: `integer` input value used by `RandomInt`.
// - `Hi`: `integer` input value used by `RandomInt`.
public function RandomInt(Lo: integer; Hi: integer): integer;
begin
  panic('Intrinsic API declaration')
end;

// Replace the VM-local state with an operating-system seed.
public procedure Randomize();
begin
  panic('Intrinsic API declaration')
end;

// Start a repeatable VM-local sequence.
//
// Parameters:
// - `Seed`: `integer` input value used by `SetSeed`.
public procedure SetSeed(Seed: integer);
begin
  panic('Intrinsic API declaration')
end;
