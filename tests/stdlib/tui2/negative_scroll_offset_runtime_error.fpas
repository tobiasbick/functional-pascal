program Tui2NegativeScrollOffsetRuntimeError;

uses Std.Tui2;

begin
  var App: TuiApplication := TuiApplication.OpenForTest(TuiSize.Create(10, 5));
  var ScrollView: TuiScrollView := TuiScrollView.Create(App);
  ScrollView.Offset := TuiPoint.Create(-1, 0)
end.
