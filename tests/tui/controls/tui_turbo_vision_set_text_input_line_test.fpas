program TuiTurboVisionSetTextInputLineTest;

uses Std.Console, Std.Tui, Std.Test;

function Bounds(X: integer; Y: integer; Width: integer; Height: integer): Rect;
begin
  return record x := X; y := Y; width := Width; height := Height; end
end;

begin
  var App: Application := Application.OpenForTest(60, 20);
  var DialogHandle: Dialog := Application.CreateDialog(App, Bounds(2, 1, 30, 8), 'Host');
  var NameInput: InputLine := Application.CreateInputLine(App, Bounds(3, 2, 20, 1), 'OLD', 32);
  Application.AddChild(App, DialogHandle, NameInput);
  Application.Pump(App);
  AssertScreenCell(7, 6, 'O', White, Green);
  Application.SetText(App, NameInput, 'NEW');
  Application.Pump(App);
  AssertScreenCell(7, 6, 'N', White, Green)
end.
