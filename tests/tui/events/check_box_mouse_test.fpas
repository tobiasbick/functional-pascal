program CheckBoxMouseTest;

uses Std.Tui, Std.Test;

function Bounds(X: integer; Y: integer; Width: integer; Height: integer): Rect;
begin
  return record x := X; y := Y; width := Width; height := Height; end
end;

procedure OnCommand(App: Application; Cmd: integer);
begin
  if Cmd = CM_QUIT then
  begin
    Application.Quit(App)
  end
end;

begin
  var App: Application := Application.OpenForTest(60, 20);
  var Win: Std.Tui.Window := Window.New(Bounds(2, 1, 30, 8), 'Host');
  var Opt: CheckBox := CheckBox.New(Bounds(3, 2, 20, 1), 'opt', false);
  Window.Add(Win, Opt);
  Desktop.Add(App, Win);
  Application.TestClickMouse(App, 5, 3);
  Test.InjectCommand(App, CM_QUIT);
  Application.Run(App, OnCommand);
  AssertTrue(CheckBox.Checked(Opt));
  Application.CloseForTest(App)
end.
